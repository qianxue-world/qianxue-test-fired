apiVersion: apps/v1
kind: Deployment
metadata:
  name: mbti-personality-test
  namespace: lovetest-mbti
  labels:
    app: mbti-personality-test
    version: v1
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: mbti-personality-test
  template:
    metadata:
      labels:
        app: mbti-personality-test
        version: v1
    spec:
      containers:
      - name: mbti-test
        image: omaticaya/lovetest-mbti:1.0.1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        env:
        - name: NODE_ENV
          value: "production"
        - name: VITE_API_BASE_URL
          valueFrom:
            configMapKeyRef:
              name: mbti-personality-test-config
              key: API_BASE_URL
      restartPolicy: Always